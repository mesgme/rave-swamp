id: 7ffc2c4d-9b41-481f-8e94-4aac697e70e3
name: gather-all-evidence
description: Gather all rave-swamp evidence in parallel — one job per evidence model instance
tags: {}
jobs:
  - name: ci-test-results
    description: Gather CI test results evidence (validate.yml run)
    steps:
      - name: fetch
        description: Fetch latest validate.yml run result
        task:
          type: model_method
          modelIdOrName: evidence-ci-test-results-001
          methodName: gather
          inputs:
            githubToken: ${{ vault.get(rave-credentials, GITHUB_TOKEN) }}
        allowFailure: true
    dependsOn: []
    weight: 0
  - name: swamp-model-validate
    description: Gather swamp model validation evidence (validate.yml run)
    steps:
      - name: fetch
        description: Fetch latest validate.yml run result for model validation
        task:
          type: model_method
          modelIdOrName: evidence-swamp-model-validate-001
          methodName: gather
          inputs:
            githubToken: ${{ vault.get(rave-credentials, GITHUB_TOKEN) }}
        allowFailure: true
    dependsOn: []
    weight: 0
  - name: swamp-workflow-validate
    description: Gather swamp workflow validation evidence (validate.yml run)
    steps:
      - name: fetch
        description: Fetch latest validate.yml run result for workflow validation
        task:
          type: model_method
          modelIdOrName: evidence-swamp-workflow-validate-001
          methodName: gather
          inputs:
            githubToken: ${{ vault.get(rave-credentials, GITHUB_TOKEN) }}
        allowFailure: true
    dependsOn: []
    weight: 0
  - name: typescript-compile
    description: Gather TypeScript compile check evidence (validate.yml run)
    steps:
      - name: fetch
        description: Fetch latest validate.yml run result for TS compile check
        task:
          type: model_method
          modelIdOrName: evidence-typescript-compile-001
          methodName: gather
          inputs:
            githubToken: ${{ vault.get(rave-credentials, GITHUB_TOKEN) }}
        allowFailure: true
    dependsOn: []
    weight: 0
  - name: branch-protection
    description: Gather GitHub branch protection policy evidence
    steps:
      - name: fetch
        description: Query GitHub branch protection API
        task:
          type: model_method
          modelIdOrName: evidence-github-branch-protection-001
          methodName: gather
          inputs:
            githubToken: ${{ vault.get(rave-credentials, GITHUB_TOKEN) }}
        allowFailure: true
    dependsOn: []
    weight: 0
  - name: actions-runs
    description: Gather GitHub Actions run history evidence
    steps:
      - name: fetch
        description: Query GitHub Actions runs API
        task:
          type: model_method
          modelIdOrName: evidence-github-actions-runs-001
          methodName: gather
          inputs:
            githubToken: ${{ vault.get(rave-credentials, GITHUB_TOKEN) }}
        allowFailure: true
    dependsOn: []
    weight: 0
  - name: lint
    description: Gather lint check evidence (validate.yml run)
    steps:
      - name: fetch
        description: Fetch latest validate.yml run result for lint check
        task:
          type: model_method
          modelIdOrName: evidence-lint-clean-001
          methodName: gather
          inputs:
            githubToken: ${{ vault.get(rave-credentials, GITHUB_TOKEN) }}
        allowFailure: true
    dependsOn: []
    weight: 0
  - name: unit-tests
    description: Gather unit test evidence (validate.yml run)
    steps:
      - name: fetch
        description: Fetch latest validate.yml run result for unit tests
        task:
          type: model_method
          modelIdOrName: evidence-unit-tests-pass-001
          methodName: gather
          inputs:
            githubToken: ${{ vault.get(rave-credentials, GITHUB_TOKEN) }}
        allowFailure: true
    dependsOn: []
    weight: 0
  - name: code-coverage
    description: Gather code coverage evidence (validate.yml run)
    steps:
      - name: fetch
        description: Fetch latest validate.yml run result for code coverage
        task:
          type: model_method
          modelIdOrName: evidence-code-coverage-001
          methodName: gather
          inputs:
            githubToken: ${{ vault.get(rave-credentials, GITHUB_TOKEN) }}
        allowFailure: true
    dependsOn: []
    weight: 0
  - name: merged-prs-reviewed
    description: Gather PR review governance evidence (governance-check.yml run)
    steps:
      - name: fetch
        description: Fetch latest governance-check.yml run result for PR review check
        task:
          type: model_method
          modelIdOrName: evidence-merged-prs-reviewed-001
  - name: no-secrets
    description: Gather secrets scan evidence (validate.yml run)
    steps:
      - name: fetch
        description: Fetch latest validate.yml run result for secrets scan
        task:
          type: model_method
          modelIdOrName: evidence-no-secrets-committed-001
          methodName: gather
          inputs:
            githubToken: ${{ vault.get(rave-credentials, GITHUB_TOKEN) }}
        allowFailure: true
    dependsOn: []
    weight: 0
  - name: main-commits-via-pr
    description: Gather commit provenance governance evidence (governance-check.yml run)
    steps:
      - name: fetch
        description: Fetch latest governance-check.yml run result for commit provenance check
        task:
          type: model_method
          modelIdOrName: evidence-main-commits-via-pr-001
  - name: no-known-cves
    description: Gather CVE scan evidence (validate.yml run)
    steps:
      - name: fetch
        description: Fetch latest validate.yml run result for CVE scan
        task:
          type: model_method
          modelIdOrName: evidence-no-known-cves-001
          methodName: gather
          inputs:
            githubToken: ${{ vault.get(rave-credentials, GITHUB_TOKEN) }}
        allowFailure: true
    dependsOn: []
    weight: 0
  - name: no-stale-untriaged-issues
    description: Gather stale issue triage governance evidence (governance-check.yml run)
    steps:
      - name: fetch
        description: Fetch latest governance-check.yml run result for stale issue check
        task:
          type: model_method
          modelIdOrName: evidence-no-stale-untriaged-issues-001
  - name: deno-lock-committed
    description: Gather deno.lock verification evidence (validate.yml run)
    steps:
      - name: fetch
        description: Fetch latest validate.yml run result for lock file check
        task:
          type: model_method
          modelIdOrName: evidence-deno-lock-committed-001
          methodName: gather
          inputs:
            githubToken: ${{ vault.get(rave-credentials, GITHUB_TOKEN) }}
        allowFailure: true
    dependsOn: []
    weight: 0
  - name: npm-imports-pinned
    description: Gather npm version-pin check evidence (validate.yml run)
    steps:
      - name: fetch
        description: Fetch latest validate.yml run result for npm pin check
        task:
          type: model_method
          modelIdOrName: evidence-npm-imports-pinned-001
          methodName: gather
          inputs:
            githubToken: ${{ vault.get(rave-credentials, GITHUB_TOKEN) }}
        allowFailure: true
    dependsOn: []
    weight: 0
version: 1
