evidence_id: "evidence-swamp-model-validate-001"
type: "ci_log"
description: "swamp model validate output for all extension models in mesgme/rave-swamp"
reference:
  uri: "https://api.github.com/repos/mesgme/rave-swamp/actions/workflows/validate.yml/runs?branch=main&per_page=1"
  format: "application/json"
  authentication:
    method: "bearer_token"
    hint: "GITHUB_TOKEN"
claim_ids:
  - "claim-swamp-models-valid-001"
freshness_window: "P1D"
quality_score: 0.98
methodology:
  description: "Run 'swamp model validate --json' against all models in the repository in a GitHub Actions workflow step; pass if totalFailed == 0"
  tooling: "github-actions"
  frequency: "P1D"
  coverage: "All extension model definitions in extensions/models/"
metadata:
  source_system: "github-actions"
  repo: "mesgme/rave-swamp"
  workflow: "validate.yml"
