claim_id: claim-lint-clean-001
statement: All extension model TypeScript files pass deno lint without errors
owner:
  name: mellens
  contact: mesgme/rave-swamp
status: active
category: code_quality
scope:
  type: component
  target: mesgme/rave-swamp/extensions
assumptions:
  - deno is available in the CI environment
  - extensions/models/ contains at least one .ts file
  - no-import-prefix rule is excluded (swamp uses inline npm: specifiers by design)
falsification_signals:
  - deno lint exits with a non-zero exit code
  - Any extension model file produces a lint error
decay_lambda: 0.05
annotations: []
